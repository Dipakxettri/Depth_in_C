
#include<stdio.h>

int main(){
    // rules
    // 1. can't start with digit ex: int 2globe = 2;
    // 2. void use of underscore _
    // 3. case sensative, var and Var is different
    // 4. special characters (@.#,%,&,*) not allowed
    // 5. white spaces are not allowed
    // 6. defult/reserved keywords like(if,else) are not allowed
    // 7. do not use long names
    return 0;

    
    
}