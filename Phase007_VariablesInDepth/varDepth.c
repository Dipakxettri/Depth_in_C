
#include<stdio.h>

int main(){
    // int a = 20;
    // variable is a container for storing values
    // in C there are different datatypes defined with different keywords and stored in variable

    int num; //decliration of variable
    
    // points:
    // variable are names that points to some memory location like (int a = 20) it will create a memory of a 4 bytes
    // the datatype given in variable defines the size of memory a variable will going to take
    // semiclon (;) is a way to seperate a lines of code for compiler

    int num2 = 20;//initialization of variable

    // variable = vary-able 

    // points:
    // each variable only be defined once and can be used and change multiple time
    int a, b, c, d;  // declare variables
    a = b = c = d = 20;  // assign 20 to all
    printf("%d\n", a);  // print the value of 'a'
    return 0;
    
    // for detailed visulation of c in memory visit:
    // https:pythontutor.com/visualize.html#mode=edit

}