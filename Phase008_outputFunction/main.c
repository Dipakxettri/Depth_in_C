
#include<stdio.h>

int main(){
    
    int a = 20;
    int b = 20;


    printf("deepak");
    printf("%d", a);//%d is a placeholder that is used for numbers - it says that take a next variable and replace me with that arguement
    printf("%d %d", a, b);
    // Note: the first character of a printf is always string
    return 0;
}